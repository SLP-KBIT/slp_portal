- @title = 'サーバー管理'

h3.bold
  = fa_stacked_icon 'hdd-o inverse', base: 'square', text: @title

= link_to new_admin_server_path, class: 'btn btn-info', remote: true do
  = fa_icon 'plus', text: '登録'

p

= render partial: 'admin/servers/list'
